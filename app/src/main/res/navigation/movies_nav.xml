<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav.xml"
    app:startDestination="@id/movie_item_fragment">

    <fragment
        android:id="@+id/movie_item_fragment"
        android:name="com.example.movies.movies.MovieFragment"
        tools:layout="@layout/movie_fragment">
        <action
            android:id="@+id/movie_detailed_action"
            app:destination="@id/activity_movie_detailed" />
    </fragment>
    <fragment
        android:id="@+id/activity_movie_detailed"
        android:name="com.example.movies.details.InfoFragment"
        tools:layout="@layout/activity_movie_detailed" />
</navigation>