<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav.xml"
    app:startDestination="@id/movie_item_fragment">

    <!-- // TODO change name  android:name="com.example.movies.MainActivity"-->
    <fragment
        android:id="@+id/movie_item_fragment"
        android:name="com.example.movies.MainActivity"
        tools:layout="@layout/movie_item_fragment">
        <action
            android:id="@+id/movie_detailed_action"
            app:destination="@id/movie_detailed" />
    </fragment>
    <fragment
        android:id="@+id/movie_detailed"
        android:name="com.example.movies.MainActivity"
        tools:layout="@layout/movie_detailed" />
</navigation>